{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "vscode-extension: compile",
      "type": "shell",
      "command": "npm run compile",
      "options": {
        "cwd": "${workspaceFolder}/vscode-extension"
      },
      "problemMatcher": [
        "$tsc"
      ],
      "group": "build"
    }
  ]
}
